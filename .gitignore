# macOS
.DS_Store
._*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code project config (local)
.claude/

# Build artefacts — .skill archives are attached to GitHub Releases, not committed
*.skill

# Internal-history archive — preserved locally but not published
archive/

# Local test outputs that live at the project root
/*-v1.html
/output/
output/
